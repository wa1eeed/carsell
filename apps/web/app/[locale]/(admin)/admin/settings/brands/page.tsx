import AdminCatalogClient from '@/app/[locale]/(admin)/admin/catalog/AdminCatalogClient'

export const metadata = { title: 'براندات السيارات — CarSell Admin' }

export default function AdminBrandsPage() {
  return <AdminCatalogClient />
}
